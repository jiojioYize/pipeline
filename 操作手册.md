# TED 演讲数据填充操作手册

## 总览

**最终目标**：补全 `seed_data.sql`，使其包含 5 张表的完整数据，然后复制粘贴到团队项目的 SQL 文件中。

**技术方案**：利用 TED 演讲在 YouTube 上自带的人工校准 SRT 字幕来获取精准时间戳，无需 GPU。

**核心流程：**

```
① python pipeline.py --mode download    ← 下载 32 个音频 + SRT 字幕
② python pipeline.py --mode parse       ← 解析字幕，合并为段落级时间戳
③ python pipeline.py --mode sql         ← 自动补全 seed_data.sql
④ 上传音频到阿里云 OSS → 替换 audio_url
⑤ 复制到团队项目 SQL 文件
```

**项目目录结构：**

```
ted-data-pipeline/
├── seed_data.sql         ← 种子数据（必须和 pipeline.py 同目录）
├── pipeline.py           ← 自动化脚本
└── ted_data/             ← 自动生成
    ├── audio/            ← 32 个 MP3
    ├── subtitles/        ← SRT 字幕文件
    └── parsed/           ← 段落解析结果 JSON
```

---

## 第一步：环境准备

### 1.1 系统要求

| 要求 | 说明 |
|------|------|
| Python | 3.8+ |
| GPU | **不需要** |
| 磁盘 | 约 300MB（音频 + 字幕） |
| 网络 | 需要能访问 YouTube |

### 1.2 安装依赖（只需要两个包）

```bash
pip install yt-dlp pysrt
```

安装 ffmpeg（用于获取音频精确时长）：

```bash
# Windows: 下载 https://ffmpeg.org/download.html 并加入 PATH
# macOS: brew install ffmpeg
# Linux: sudo apt install ffmpeg
```

验证：

```bash
yt-dlp --version
python -c "import pysrt; print('pysrt OK')"
ffmpeg -version
```

### 1.3 放置文件

```bash
mkdir ted-data-pipeline
cd ted-data-pipeline
# 把 seed_data.sql 和 pipeline.py 复制到这里
```

---

## 第二步：下载音频和字幕

```bash
python pipeline.py --mode download
```

32 个 MP3 + 32 个 SRT 字幕文件，共约 200MB。

**⚠️ 常见问题：**

| 问题 | 解决方案 |
|------|---------|
| 搜索到错误视频 | 在 YouTube 手动搜索标题，找到正确视频后复制 URL，替换 pipeline.py 中 TALKS 字典对应条目的 `search` 值为完整 URL（如 `https://www.youtube.com/watch?v=XXXXX`） |
| 网络超时 | 使用代理：在 pipeline.py 的 yt-dlp 命令列表中加 `"--proxy", "socks5://127.0.0.1:1080"` |
| 某个视频失败 | 脚本会跳过已下载的文件，修复后重新运行即可 |
| yt-dlp 报错 | `pip install --upgrade yt-dlp` 升级到最新版 |
| 字幕下载失败 | 少数 TED 视频可能没有英文字幕，需要手动处理（见附录 A） |

**下载后检查：**

```bash
# 文件数量
ls ./ted_data/audio/*.mp3 | wc -l       # 应为 32
ls ./ted_data/subtitles/*.srt | wc -l   # 应为 32（可能略少）

# 是否有空文件
find ./ted_data/audio/ -name "*.mp3" -size 0
find ./ted_data/subtitles/ -name "*.srt" -size 0
```

---

## 第三步：解析 SRT 字幕

```bash
python pipeline.py --mode parse
```

**几秒钟即可完成。** 结果输出到 `./ted_data/parsed/`，每篇文章一个 JSON。

**原理**：读取 SRT 中每条字幕的精确时间戳，当两条字幕之间的停顿超过 2 秒时视为段落分界，将该段落内所有字幕文本合并，取首条的 start 和末条的 end 作为段落时间戳。

**调整段落粒度（可选）：**

```bash
# 更长段落（停顿 > 3 秒才分段）
python pipeline.py --mode parse --pause-threshold 3.0
# 更短段落（停顿 > 1.5 秒就分段）
python pipeline.py --mode parse --pause-threshold 1.5
```

> ⚠️ 调整前先删除 `./ted_data/parsed/` 目录，否则会跳过已解析的文件。

**抽查结果：**

```bash
python -c "
import json
d = json.load(open('./ted_data/parsed/01_golden-gate-bridge.json'))
for i, p in enumerate(d['paragraphs']):
    print(f'段落{i+1} [{p[\"start_time\"]}s - {p[\"end_time\"]}s]: {p[\"text\"][:80]}...')
"
```

---

## 第四步：补全 seed_data.sql

```bash
python pipeline.py --mode sql
```

脚本直接修改 `seed_data.sql`：
- 回填 `articles` 表中 `word_count` 和 `total_duration` 的真实值
- 在文件末尾追加 `article_segments` 的完整 INSERT 语句

> ⚠️ **不要重复运行**，否则 article_segments 会被追加两次。如需重跑，先恢复原始 seed_data.sql。

---

## 第五步：上传音频到阿里云 OSS

### 5.1 创建 Bucket

登录 [OSS 控制台](https://oss.console.aliyun.com/) → 创建 Bucket：

| 配置项 | 推荐值 |
|--------|--------|
| Bucket 名称 | `english-reading-audio`（自定义，全局唯一） |
| 地域 | 和你们服务器同地域 |
| 存储类型 | 标准存储 |
| 读写权限 | **公共读**（前端播放器需直接访问） |

### 5.2 获取 AccessKey

[RAM 控制台](https://ram.console.aliyun.com/) → 创建子用户 → 勾选 OpenAPI 访问 → 授权 AliyunOSSFullAccess → 保存 AK/SK。

> ⚠️ AccessKey Secret 只展示一次，务必立即保存。

### 5.3 安装 ossutil 并上传

```bash
# 下载 ossutil（以 Linux 为例）
curl -o ossutil.zip https://gosspublic.alicdn.com/ossutil/v2/2.1.2/ossutil-2.1.2-linux-amd64.zip
unzip ossutil.zip && sudo mv ossutil /usr/local/bin/

# 配置（按提示输入 AK、SK、Region ID）
ossutil config

# 批量上传
ossutil cp ./ted_data/audio/ oss://english-reading-audio/audio/ -r -f

# 验证
ossutil ls oss://english-reading-audio/audio/ | head -5
```

### 5.4 配置 CORS（重要！否则前端播放报跨域错误）

OSS 控制台 → Bucket → 数据安全 → 跨域设置 → 创建规则：

| 配置项 | 值 |
|--------|-----|
| 来源 | `*` |
| 允许 Methods | `GET` |
| 允许 Headers | `*` |

### 5.5 替换 audio_url

```bash
# Linux:
sed -i 's|https://your-storage.com/audio/|https://english-reading-audio.oss-cn-hangzhou.aliyuncs.com/audio/|g' seed_data.sql
# macOS:
sed -i '' 's|https://your-storage.com/audio/|https://english-reading-audio.oss-cn-hangzhou.aliyuncs.com/audio/|g' seed_data.sql
```

> ⚠️ seed_data.sql 中的路径格式（如 `ce/golden-gate-bridge.mp3`）和下载的实际文件名（如 `01_golden-gate-bridge.mp3`）不一致。上传后需确保 OSS 路径和 SQL 中的 audio_url 匹配，必要时手动逐条替换。

**验证**：在浏览器打开某个 audio_url，确认能播放。

---

## 第六步：复制到团队项目

将 seed_data.sql 中各表 INSERT 语句复制粘贴到团队 SQL 文件。

**粘贴顺序**（遵循外键依赖）：tags → articles → article_tags → users → article_segments

---

## 第七步：补充中文翻译（3-5 篇，手动）

在 seed_data.sql 中找到对应 article_segments 的 INSERT，把 `NULL` 替换为中文翻译。注意单引号转义为 `''`。

---

## 一键执行

```bash
python pipeline.py --mode all   # download → parse → sql
```

---

## 附录 A：字幕缺失的处理

极少数视频可能没有 SRT 字幕。手动在 ted.com 复制 Transcript，分段记录时间戳，写 JSON 放入 `./ted_data/parsed/`：

```json
{"article_id": 1, "slug": "xxx", "paragraphs": [
  {"text": "段落文本...", "start_time": 0, "end_time": 25}
]}
```

## 附录 B：OSS 费用

存储 200MB 每月约 ¥0.02，每 1000 次播放流量约 ¥2.5。开发阶段几乎不花钱。

## 附录 C：问题排查

| 步骤 | 问题 | 解决 |
|------|------|------|
| 下载 | 搜到错误视频 | TALKS 字典 search 改为完整 URL |
| 下载 | 全部失败 | `pip install --upgrade yt-dlp` |
| 解析 | 段落太碎/太长 | 删 parsed 目录，改 `--pause-threshold` 重跑 |
| SQL | 找不到 seed_data.sql | 确认两文件同目录 |
| SQL | 数据重复 | 恢复原始 seed_data.sql 后重跑 |
| OSS | AccessDenied | Bucket 设为公共读 |
| OSS | 前端跨域错误 | 配置 CORS（见 5.4） |
